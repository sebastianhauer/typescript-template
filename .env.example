# This is an example environment configuration file.
# To use this file:
# 1. Make a copy of this file and name it '.env'
# 2. Replace the values with your actual configuration
# Note: .env files should never be committed to version control

# Server Configuration, for example:
PORT=3000               # The port number the server will listen on
NODE_ENV=dev            # Environment mode: 'dev', 'test', or 'prod'

# Add other environment variables below, for example:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# API_KEY=your_api_key_here
